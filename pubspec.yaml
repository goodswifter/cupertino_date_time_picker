name: cupertino_date_time_picker
description: 日期时间选择器
version: 0.0.4
homepage: https://github.com/goodswifter/cupertino_date_time_picker.git

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
